# StreamPay AI — Root Environment Reference
#
# Each module has its own .env.example with details:
#   backend/.env.example        — API server config
#   frontend/.env.example       — Next.js public vars
#   streampay-eliza/.env.example — ElizaOS agent config
#   smart-contracts/.env.example — Hardhat / deployment config
#
# Below are the most common shared variables for quick setup:

# Database
DATABASE_URL=postgresql://postgres@localhost:5432/streampay

# Blockchain
NETWORK=sepolia
PRIVATE_KEY=your-private-key

# AI Provider (Google Gemini)
GOOGLE_GENERATIVE_AI_API_KEY=your-google-api-key

# Ports (defaults)
# Backend:         3001
# Eliza Agent:     3002
# Frontend:        3003
